name: Pull Request - Test

on:
  pull_request:
    branches: [master]
jobs:
  PR:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm install
      - run: npm test
